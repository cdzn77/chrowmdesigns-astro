---
import BaseLayout from '../layouts/BaseLayout.astro';
import HeroVideo from '../components/HeroVideo.astro';
import StatsMarquee from '../components/StatsMarquee.astro';
import CaseCard from '../components/CaseCard.astro';
import HireMarquee from '../components/HireMarquee.astro';
import SkillCard from '../components/SkillCard.astro';
import CTABanner from '../components/CTABanner.astro';
import Footer from '../components/Footer.astro';
import cases from '../data/cases.json';

// Homepage shows the first 6 cases
const featuredCases = cases.slice(0, 6);

const skills = [
  {
    number: '001',
    title: 'Strategic Approach',
    description:
      'I align design with your brand, audience, and business goals to create strategic, user-focused digital experiences that deliver measurable results.',
    image: '/images/strategic_approach_thumb.webp',
  },
  {
    number: '002',
    title: 'User Experience',
    description:
      'Delivering excellent user experiences is essential for building trust and is central to my approach, which focuses on intuitive functionality, simplicity, and delight.',
    image: '/images/user_experience_thumb.webp',
  },
  {
    number: '003',
    title: 'Interactive Design',
    description:
      'Design is central to my work, where I combine creativity and expertise to achieve outstanding results.',
    image: '/images/interactive_design_thumb.webp',
  },
  {
    number: '004',
    title: 'Digital Experiences',
    description:
      'I design end-to-end product journeys that balance usability, emotional impact, and brand expression across web and digital platforms.',
    image: '/images/digital_experiences_thumb.webp',
  },
];
---

<BaseLayout
  title="ChrowmDesigns"
  description="Angelo Manzano Jr. — Senior Product Designer with 20 years of experience crafting digital experiences that connect, engage, and inspire."
>

  <!-- Organization structured data -->
  <script type="application/ld+json" slot="head" set:html={JSON.stringify({
    "@context": "https://schema.org",
    "@type": "Organization",
    "name": "ChrowmDesigns",
    "alternateName": "CHRØWM Designs",
    "url": "https://chrowmdesigns.com",
    "logo": "https://chrowmdesigns.com/images/logo-1.webp",
    "description": "Independent design studio specializing in UX strategy, brand design, and digital product experiences.",
    "foundingDate": "1999",
    "founder": {
      "@type": "Person",
      "name": "Angelo Manzano Jr.",
      "jobTitle": "Senior UX Strategist & Creative Director",
      "url": "https://chrowmdesigns.com/about"
    },
    "address": {
      "@type": "PostalAddress",
      "addressRegion": "FL",
      "addressCountry": "US"
    },
    "contactPoint": {
      "@type": "ContactPoint",
      "contactType": "business",
      "email": "chile@chrowmdesigns.com"
    },
    "sameAs": [
      "https://www.linkedin.com/in/angelomanzano"
    ]
  })} />

  <!-- 1. Hero -->
  <HeroVideo />

  <!-- 2. Stats Marquee -->
  <StatsMarquee />

  <!-- 3. Featured Image -->
  <section class="featured-image" aria-label="Featured work">
    <div class="featured-image__img-wrap">
      <img
        src="/images/marriott_tablet_home.webp"
        alt="Marriott Vacation Club digital experience on tablet — enterprise UX redesign"
        class="featured-image__img"
        width="1620"
        height="850"
        loading="lazy"
        decoding="async"
      />
    </div>
    <div class="featured-image__caption">
      <p class="featured-image__text">
        Crafting digital experiences that connect, engage and inspire.
      </p>
      <a href="/cases" class="featured-image__link">
        Browse all cases
        <svg width="14" height="14" viewBox="0 0 14 14" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
          <path d="M2 7H12M12 7L7 2M12 7L7 12" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
        </svg>
      </a>
    </div>
  </section>

  <!-- 4. Case Study Section Header -->
  <section class="cases-header" aria-labelledby="cases-title">
    <div class="cases-header__title-block">
      <h2 class="cases-header__title" id="cases-title">
        Crafting digital experiences<br />
        that connect, engage and inspire.
      </h2>
    </div>

    <div class="cases-header__descriptions">
      <div class="cases-header__description-left">
        <p class="cases-header__label">Project Focus</p>
        <p class="cases-header__text">
          I work with brands, product teams, and<br />
          platforms that need design to do more than<br />
          look good. Every project starts with the<br />
          problem, not the pixels.
        </p>
      </div>

      <div class="cases-header__description-right">
        <p class="cases-header__label">Approach</p>
        <p class="cases-header__text">
          From first conversation to final delivery, the work stays grounded in real user behavior, business context, and decisions that hold up. Every experience ships with purpose and a reason behind each choice.
        </p>
      </div>
    </div>

    <p class="cases-header__index">[CDN = 01]</p>
  </section>

  <!-- 5. Case Study Grid - Masonry Layout -->
  <section class="cases-grid" aria-labelledby="cases-heading">
    <h2 class="visually-hidden" id="cases-heading">Featured Case Studies</h2>
    <div class="cases-grid__inner">
      {featuredCases.map((c, i) => (
        <CaseCard
          title={c.title}
          category={c.category}
          thumbnail={c.thumbnail}
          href={c.url}
          size={i < 2 ? 'large' : 'small'}
          offset={i % 2 === 1}
          loading={i < 2 ? 'eager' : 'lazy'}
        />
      ))}
    </div>
    <div class="cases-grid__footer">
      <p class="cases-grid__tagline">Florida-based, freelance, multidisciplinary designer studio</p>
      <a href="/cases" class="cases-grid__all-link">
        Explore more work
        <svg width="14" height="14" viewBox="0 0 14 14" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
          <path d="M2 7H12M12 7L7 2M12 7L7 12" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
        </svg>
      </a>
    </div>
  </section>

  <!-- 5. Hire Marquee -->
  <HireMarquee />

  <!-- 6. The Studio -->
  <section class="studio" aria-labelledby="studio-heading">
    <div class="studio__inner">
      <div class="studio__label">The studio</div>
      <h2 class="studio__heading" id="studio-heading">
        I'm an independent design studio blending creativity and strategy to
        craft standout digital products since 2000.
      </h2>
      <div class="studio__links">
        <a href="/about" class="studio__link">
          Learn More
          <svg width="14" height="14" viewBox="0 0 14 14" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
            <path d="M2 7H12M12 7L7 2M12 7L7 12" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
        </a>
        <a href="/cases" class="studio__link">
          Browse all cases
          <svg width="14" height="14" viewBox="0 0 14 14" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
            <path d="M2 7H12M12 7L7 2M12 7L7 12" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
        </a>
      </div>
    </div>
  </section>

  <!-- 7. Portrait Section -->
  <section class="portrait" aria-label="About Angelo Manzano Jr.">
    <div class="portrait__img-wrap">
      <img
        src="/images/foot_top.webp"
        alt="Angelo Manzano Jr. — Senior Product Designer and UX Strategist"
        class="portrait__img"
        width="1620"
        height="850"
        loading="lazy"
        decoding="async"
      />
      <div class="portrait__overlay" aria-hidden="true">
        <span class="portrait__wordmark">ChrowmDesi&#x2014;</span>
      </div>
    </div>
  </section>

  <!-- 8. Skills Section -->
  <section class="skills" aria-labelledby="skills-heading">
    <div class="skills__header">
      <h2 class="skills__heading" id="skills-heading">
        Skills To Pay The Bills
      </h2>
    </div>
    <div class="skills__grid">
      {skills.map((skill) => (
        <SkillCard
          number={skill.number}
          title={skill.title}
          description={skill.description}
          image={skill.image}
        />
      ))}
    </div>
  </section>

  <!-- 9. CTA Banner -->
  <CTABanner />

  <!-- 10. Footer -->
  <Footer />

</BaseLayout>

<style>
  /* ── Featured Image ─────────────────────────────── */
  .featured-image {
    display: none;  /* Hidden in React design */
    background-color: var(--color-primary);
  }

  .featured-image__img-wrap {
    width: 100%;
    overflow: hidden;
    max-height: 80vh;
  }

  .featured-image__img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }

  .featured-image__caption {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 2rem;
    flex-wrap: wrap;
    padding: 3rem clamp(1.5rem, 5vw, 5rem);
    border-bottom: 1px solid rgba(255, 255, 255, 0.08);
  }

  .featured-image__text {
    font-family: var(--font-sans);
    font-size: clamp(1.2rem, 2.5vw, 2rem);
    font-weight: 400;
    letter-spacing: -0.02em;
    color: var(--color-white);
    margin: 0;
    max-width: 600px;
  }

  .featured-image__link {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 0.8rem;
    font-weight: 600;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--color-accent);
    text-decoration: none;
    white-space: nowrap;
    transition: gap 0.2s ease;
  }

  .featured-image__link:hover {
    gap: 0.8rem;
  }

  /* ── Cases Header ────────────────────────────────── */
  .cases-header {
    width: 100%;
    background-color: #ffffff;
    padding-top: clamp(60px, 12vw, 120px);
    padding-bottom: clamp(40px, 8vw, 80px);
    padding-left: 40px;
    padding-right: 40px;
    box-sizing: border-box;
  }

  .cases-header__title-block {
    margin-bottom: clamp(40px, 10vw, 80px);
    text-align: center;
  }

  .cases-header__title {
    font-size: clamp(28px, 7vw, 48px);
    font-weight: 400;
    color: #000000;
    margin: 0 0 clamp(40px, 8vw, 80px) 0;
    font-family: 'Manrope', sans-serif;
    text-transform: none;
    letter-spacing: -0.01em;
    line-height: 1.2;
  }

  .cases-header__descriptions {
    display: flex;
    flex-direction: row;
    gap: clamp(40px, 8vw, 80px);
    justify-content: space-between;
    margin: 0 -40px;
    padding: 0 40px;
  }

  .cases-header__description-left {
    flex: 0 0 auto;
    text-align: left;
    max-width: calc(50% - 20px);
  }

  .cases-header__description-right {
    flex: 0 0 auto;
    text-align: right;
    max-width: calc(50% - 20px);
  }

  .cases-header__label {
    font-size: 11px;
    font-weight: 600;
    color: #000000;
    margin: 0 0 12px 0;
    font-family: 'JetBrains Mono', monospace;
    text-transform: uppercase;
    letter-spacing: 0.6px;
  }

  .cases-header__text {
    font-size: 13px;
    font-weight: 400;
    color: #555555;
    margin: 0;
    font-family: 'JetBrains Mono', monospace;
    line-height: 1.7;
  }

  .cases-header__index {
    font-size: 11px;
    font-weight: 400;
    color: #888888;
    margin: clamp(40px, 8vw, 80px) 0 0 0;
    font-family: 'JetBrains Mono', monospace;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    text-align: left;
  }

  /* ── Cases Grid ──────────────────────────────────── */
  .cases-grid {
    padding: 0 40px;
    background-color: #ffffff;
    width: 100%;
    box-sizing: border-box;
  }

  .cases-grid__inner {
    display: flex;
    flex-wrap: wrap;
    gap: 0;
    width: 100%;
    margin: 0;
    padding: 0;
  }

  /* Large cards: 50% width */
  :global(.case-card--large) {
    width: 50%;
    border-radius: 0;
  }

  /* Small cards: 25% width */
  :global(.case-card--small) {
    width: 25%;
    border-radius: 0;
  }

  .cases-grid__footer {
    padding: clamp(40px, 8vw, 60px) clamp(16px, 5vw, 60px);
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 2rem;
    flex-wrap: wrap;
  }

  .cases-grid__tagline {
    font-size: 13px;
    font-weight: 400;
    color: #555555;
    margin: 0;
    font-family: 'JetBrains Mono', monospace;
    letter-spacing: 0.3px;
    text-transform: uppercase;
  }

  .cases-grid__all-link {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 11px;
    font-weight: 600;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: #1a1a1a;
    text-decoration: none;
    padding: 0.8rem 2rem;
    border: 1px solid rgba(0, 0, 0, 0.1);
    border-radius: 100px;
    transition: border-color 0.2s ease, color 0.2s ease, gap 0.2s ease;
    white-space: nowrap;
  }

  .cases-grid__all-link:hover {
    border-color: #e02b10;
    color: #e02b10;
    gap: 0.8rem;
  }

  @media (max-width: 640px) {
    .cases-grid__inner {
      grid-template-columns: 1fr;
    }
    /* Reset offset on single column */
    :global(.case-card--offset) {
      margin-top: 0;
    }
  }

  /* ── Studio ──────────────────────────────────────── */
  .studio {
    padding: clamp(80px, 15vw, 120px) clamp(16px, 5vw, 60px);
    background-color: #1B1B1B;
    margin: 0;
  }

  .studio__inner {
    max-width: 900px;
    margin-left: auto;
    margin-right: auto;
  }

  .studio__label {
    font-size: 11px;
    font-weight: 600;
    letter-spacing: 0.6px;
    text-transform: uppercase;
    color: #60D3B8;
    margin-bottom: 24px;
    margin: 0 0 24px 0;
    font-family: 'JetBrains Mono', monospace;
  }

  .studio__heading {
    font-family: 'Manrope', sans-serif;
    font-size: clamp(24px, 5vw, 42px);
    font-weight: 400;
    letter-spacing: -0.01em;
    line-height: 1.3;
    color: #ffffff;
    margin: 0 0 32px 0;
  }

  .studio__links {
    display: flex;
    gap: 32px;
    flex-wrap: wrap;
  }

  .studio__link {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    font-size: 11px;
    font-weight: 600;
    letter-spacing: 0.6px;
    text-transform: uppercase;
    color: #ffffff;
    text-decoration: none;
    transition: color 0.2s ease;
    font-family: 'JetBrains Mono', monospace;
  }

  .studio__link:hover {
    color: #60D3B8;
  }

  /* ── Portrait ────────────────────────────────────── */
  .portrait {
    position: relative;
    background-color: #000000;
    overflow: hidden;
    width: 100%;
    margin: 0;
  }

  .portrait__img-wrap {
    position: relative;
    width: 100%;
    background-color: #000000;
  }

  .portrait__img {
    width: 100%;
    height: auto;
    max-height: 100vh;
    object-fit: cover;
    display: block;
  }

  .portrait__overlay {
    position: absolute;
    inset: 0;
    display: flex;
    align-items: flex-end;
    padding: clamp(40px, 8vw, 80px);
    background: linear-gradient(
      to top,
      rgba(0, 0, 0, 0.8) 0%,
      transparent 50%
    );
    pointer-events: none;
  }

  .portrait__wordmark {
    font-family: 'Manrope', sans-serif;
    font-size: clamp(60px, 20vw, 200px);
    font-weight: 700;
    letter-spacing: -0.03em;
    color: rgba(255, 255, 255, 0.15);
    line-height: 0.9;
    user-select: none;
    margin: 0;
  }

  /* ── Skills ──────────────────────────────────────── */
  .skills {
    padding: clamp(100px, 15vw, 140px) clamp(16px, 5vw, 60px);
    background-color: #000000;
    margin: 0;
  }

  .skills__header {
    margin-bottom: clamp(60px, 12vw, 100px);
    max-width: 1200px;
    margin-left: auto;
    margin-right: auto;
  }

  .skills__heading {
    font-family: 'Manrope', sans-serif;
    font-size: clamp(32px, 7vw, 56px);
    font-weight: 400;
    letter-spacing: -0.02em;
    color: #ffffff;
    margin: 0;
    line-height: 1.2;
  }

  .skills__grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 24px;
    max-width: 1400px;
    margin-left: auto;
    margin-right: auto;
  }

  @media (max-width: 1024px) {
    .skills__grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 640px) {
    .skills__grid {
      grid-template-columns: 1fr;
    }
  }
</style>
